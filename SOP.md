# 通用项目开发规范 (S.O.P.)

**核心指令:**

- 全程用中文输出
- 本文档是所有项目的标准作业程序。作为AI，你必须严格遵循
- 语言风格要求极简，禁止使用任何比喻或不必要的描述性语言

## 阶段一：项目初始化

### 1.1 需求分析
分析我提供的《项目需求规格说明书》。明确核心功能、目标用户和MVP。如有歧义，必须提问。

### 1.2 环境设置
执行以下命令，安装并配置MCP客户端。

```bash
npx -y @mcp_hub_org/cli@latest install @playwright/mcp --client claude-code
```

## 阶段二：技术栈选型

根据项目需求，遵循以下决策树选择技术。

### 2.1 前端框架
- **Web应用**: Next.js
- **移动应用**: Expo

### 2.2 后端语言
- **IF** 项目为Next.js驱动且后端逻辑轻量, **THEN** 使用Node.js + TypeScript (利用Next.js API路由)
- **IF** 项目为数据密集型或需要机器学习, **THEN** 使用Python (配合FastAPI)
- **IF** 项目为企业级，要求高性能/高并发, **THEN** 使用Java (配合Spring Boot)

### 2.3 数据库
- **首选**: Supabase
- **备选1**: Railway (需要PostgreSQL之外的数据库或更灵活的容器化时)
- **备选2**: MySQL (仅在明确要求时)

### 2.4 部署平台
- **IF** 面向海外用户 (Next.js/Python项目), **THEN** 使用Vercel
- **IF** 面向国内用户或非Vercel兼容项目 (如Java), **THEN** 使用阿里云或腾讯云

### 2.5 UI与样式库
- **基础样式**: Tailwind CSS (必须使用)
- **组件库 (首选)**: Shadcn/ui。基于Radix UI，提供高可定制性
- **组件库 (补充)**:
  - NextUI: 追求快速、开箱即用的美观界面时
  - Aceternity UI: 需要现代科技感、复杂动效的界面时
- **动画库**: Motion One (Motion.dev)
- **响应式验证**: 按我会使用Responsive Viewer插件的标准进行开发

### 2.6 API集成
- **LLM调用**: 在项目根目录创建.env.local文件，配置OPENROUTER_API_KEY。通过process.env.OPENROUTER_API_KEY读取，严禁硬编码

## 阶段三：开发流程

严格遵循以下开发顺序：

1. **数据结构设计**: 完成数据库表结构 schema 设计
2. **后端API开发**: 开发并测试所有API端点
3. **前端页面实现**: 使用UI库构建静态页面和组件
4. **功能连接调试**: 连接前后端，实现完整业务逻辑

## 阶段四：测试与迭代

### 4.1 测试体系

#### 自动化测试清单

**基础测试 (必做):**
- 单元测试 (关键业务逻辑)
- API测试 (所有端点)
- 用户流程测试 (核心路径E2E)

**进阶测试 (按需):**
- 边界测试
- 基础安全扫描
- 性能测试

#### 测试指令模板
```
请对当前代码进行全面测试：

1. 生成并运行所有单元测试
2. 测试所有API端点
3. 模拟用户完整操作流程
4. 执行基础安全检查
5. 输出测试报告和需修复问题
```

### 4.2 版本管理与迭代

#### 版本号规范
v(主版本号).(新功能号).(修复号)
- v1.0.0: MVP
- v1.0.x: Bug修复
- v1.x.0: 体验优化或小功能
- v2.0.0: 重大新功能或架构变更

#### 新功能开发流程
1. 接收我的功能需求
2. 分析对现有代码的影响范围
3. 确认后实施改动
4. 运行完整测试，确保原有功能正常

## 阶段五：部署

### 5.1 部署前检查清单
- [ ] 所有测试通过
- [ ] 敏感信息已从代码中移除并配置为环境变量
- [ ] 生产环境配置正确
- [ ] 数据已备份

### 5.2 部署指令模板
```
请为本项目生成完整的部署指南，包含：

1. 环境要求
2. 详细部署步骤
3. 环境变量配置说明
4. 常见问题解决方法
```

---

**重要提醒**: 本文档为本次编程的最高准则，所有开发活动必须严格遵循。